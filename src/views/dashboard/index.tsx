import React from 'react';
import { HeroImage } from '@components';

export const DashboardView = () => {
    return (
        <div>
            Dashboard view
            <HeroImage />
        </div>
    );
};
